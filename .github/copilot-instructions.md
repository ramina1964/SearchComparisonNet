# Copilot Instructions

## Project Guidelines
- All changes, including documentation-only changes, should go through pull requests (PRs) rather than being pushed directly to the main branch. Do not push directly to main—create a feature branch and open a PR.